{
  "name": "shaareiavodahwebsite",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test functions/lib/**/*.test.js",
    "dev": "wrangler pages dev",
    "db:migrate": "wrangler d1 execute DB --local --file=schema.sql",
    "db:pull-prod": "wrangler d1 export shaareiavodah --remote --output=.wrangler/prod-dump.sql --table schedule_overrides --table announcements --table subscribers --table send_log && wrangler d1 export shaareiavodah --remote --output=.wrangler/prod-attachments-schema.sql --table attachments --no-data && wrangler d1 execute DB --local --file=.wrangler/prod-dump.sql && wrangler d1 execute DB --local --file=.wrangler/prod-attachments-schema.sql"
  },
  "dependencies": {
    "@hebcal/core": "^6.2.0",
    "@pdf-lib/fontkit": "^1.1.1",
    "docx": "^9.6.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
